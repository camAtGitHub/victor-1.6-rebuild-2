# Console var mapping session — 2026-07-31

## Did

- Diffed engine `:8888` dump vs `cvcatalog/vars/*` (~194 documented → started from unmapped clusters).
- **Cluster:** vision motion + laser internals (opaque detector tuning, not WebViz-obvious).
- **Shard:** `resources/webserver/cvcatalog/vars/engine-vision-motion-laser.json` (35 entries).
- Registered in `cvcatalog/index.json`.

### Entries

| Group | Count | Status notes |
|---|---:|---|
| `Motion` VisionMode | 1 | live — enables detector path |
| `MotionDetection_*` + `MotionDetectionDebug` | 17 | all live; stability gates + ratio/blur/centroid/peripheral |
| `Lasers` VisionMode | 1 | live; still gated by `FeatureType::Laser` |
| `Laser_*` + `LaserDetectionDebug` | 16 | all live; dual thresholds, dark ring, color sat |

### Dead / partial / danger

- None marked dead.
- `Lasers`: live mode flag but detection no-ops if feature gate off or ground plane not visible (`statusNote` on entry).

### Evidence roots

- `engine/vision/motionDetector.cpp` (+ `.h`, `motionDetector_neon.h`)
- `engine/vision/laserPointDetector.cpp`
- `engine/vision/visionSystem.cpp` (mode enable)
- `clad/src/clad/types/visionModes.clad`

## Counts after session

- Engine vars documented **229** / unmapped **571** of **800** (approx).

## Suggested next clusters

1. **`Markers_*` + `MarkerDetector_*`** (marker crop/scale/full-frame modes) — ~10
2. **`PowerSave_*`** (6) — system power path in `powerStateManager.cpp`
3. **`RTS_*`** (6) — SoundReaction / mic direction history
4. **`BPDB_*`** (5) — PutDownBlock backup distances
5. **`VisionBenchmark_*`** (6) — benchmark harness
6. Large **`LED1_`–`LED4_`** cube light packs if desired later
